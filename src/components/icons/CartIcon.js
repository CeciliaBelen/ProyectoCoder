import React from 'react';
import Cart from '../../utils/img/cart-icon.svg'

const CartIcon = () => {
    return (
        <img src={Cart} alt="Ícono del carrito de compras" className="cartImg"></img>
    )
}

export default CartIcon